# Core
anthropic>=0.40.0
langgraph>=0.2.50
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
python-multipart>=0.0.9   # FastAPI Form parsing for Twilio webhook

# MCP
mcp>=1.0.0

# Retrieval (BM25 default; hybrid is opt-in via PATHWAYS_RETRIEVAL_BACKEND=hybrid)
rank-bm25>=0.2.2
numpy>=1.26.0
sentence-transformers>=3.0.0  # BGE-small for hybrid retrieval; CPU-only torch is bundled

# Phase 1: stateful intake, Twilio, Postgres checkpointer
twilio>=9.3.0
langgraph-checkpoint-sqlite>=2.0.0
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.2.0   # 3.2+ required by langgraph-checkpoint-postgres (PGcancelConn)
psycopg-pool>=3.2.0       # connection pool for long-lived PostgresSaver

# Observability
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
structlog>=24.4.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
